---
phase: "01-foundation-core-ui"
plan: "02"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "VitalPaw/src/store/petStore.ts"
  - "VitalPaw/src/types/index.ts"
autonomous: true
requirements:
  - "PETP-01"
  - "PETP-02"
  - "PETP-03"

must_haves:
  truths:
    - "User can add a pet with name, species, breed, birthdate, weight"
    - "User can edit existing pet profile information"
    - "User can view pet profile summary on dashboard"
  artifacts:
    - path: "VitalPaw/src/store/petStore.ts"
      provides: "Pet state management with CRUD operations"
      exports: ["usePetStore", "addPet", "updatePet", "deletePet", "selectPet"]
    - path: "VitalPaw/src/types/index.ts"
      provides: "Pet type definitions"
      exports: ["Pet", "PetSpecies"]
---

<objective>
Implement pet profile management (PETP-01, PETP-02, PETP-03). User can add pets, edit profiles, and view pet summary on dashboard. This plan focuses on the data layer and store logic - the DashboardScreen already has UI for pet management.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
## Existing Implementation

### PetStore (VitalPaw/src/store/petStore.ts)
Already implements:
- addPet, updatePet, deletePet, selectPet operations
- AsyncStorage persistence via persistPets/loadPets
- selectedPet state for current active pet

### Types (VitalPaw/src/types/index.ts)
Already defines:
```typescript
export type PetSpecies = 'dog' | 'cat' | 'bird' | 'rabbit' | 'hamster' | 'other';

export interface Pet {
  id: string;
  name: string;
  species: PetSpecies;
  breed: string;
  birthdate: Date;
  weight: number;
  photoUri?: string;
  ownerId: string;
  createdAt: Date;
  updatedAt: Date;
}
```

### Dashboard (VitalPaw/src/screens/DashboardScreen.tsx)
Already has UI for:
- Pet selection (horizontal scroll of pet cards)
- Add Pet modal with form fields
- Edit Pet modal pre-populated with existing data
- Pet summary display with name, age, weight, species

## What's Missing / Needs Verification

1. The pet store may need validation in addPet/updatePet
2. Pet data loading on app start needs verification (loadPets called on mount)
3. Birthdate handling may need date parsing for string -> Date conversion
4. Weight validation (must be positive number)

## Task Specifications

### Task 1: Enhance Pet Store Validation
**Files:** `VitalPaw/src/store/petStore.ts`

**Action:**
1. Read existing petStore.ts
2. Add validation to addPet:
   - Validate required fields: name (non-empty), species (valid PetSpecies), weight (positive number)
   - Generate proper ID using crypto.randomUUID() or similar
   - Set ownerId from auth context (pass as parameter since store doesn't have auth)
3. Add validation to updatePet:
   - Validate weight is positive number if provided
   - Validate birthdate is a valid date if provided
4. Add error handling with proper error messages
5. Ensure date fields (birthdate, createdAt, updatedAt) are properly serialized to ISO string for AsyncStorage and deserialized back to Date objects on load
6. Add getPetById helper that returns undefined if not found (already exists, verify)

**Verify:**
- `npm test -- --testPathPattern="petStore" --passWithNoTests`
- Manual: Add pet with invalid weight (negative) should show error

### Task 2: Ensure Pet Loading on App Start
**Files:** `VitalPaw/src/screens/DashboardScreen.tsx`, `VitalPaw/App.tsx`

**Action:**
1. Verify App.tsx wraps with AuthProvider (for pet store access)
2. Verify DashboardScreen calls loadPets in useEffect
3. Verify DashboardScreen calls loadEntries for health data
4. Ensure the initial load sequence:
   - AuthContext checks for existing session
   - Once authenticated, Dashboard mounts and calls loadPets
   - Pets are loaded from AsyncStorage and deserialized properly

**Verify:**
- App loads, after login, pets appear in dashboard
- Pet data persists across app restarts

### Task 3: Type Test Stub
**Files:** `VitalPaw/src/store/__tests__/petStore.test.ts` (create if not exists)

**Action:**
1. Create test file for petStore
2. Test addPet with valid data returns new pet with id
3. Test updatePet modifies correct fields
4. Test deletePet removes pet from list
5. Test selectPet sets selectedPet
6. Test loading/persistence (may need mock AsyncStorage)

**Verify:**
- `npm test -- --testPathPattern="petStore.test"`
</task>

<verification>
- Unit tests pass for petStore operations
- Manual verification: Add pet, close app, reopen, pet still exists
</verification>

<success_criteria>
- [ ] addPet creates pet with name, species, breed, birthdate, weight
- [ ] updatePet modifies existing pet fields
- [ ] Dashboard displays pet summary (name, age, weight, species)
- [ ] Pet data persists across app restarts
- [ ] No console.log statements
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-ui/01-02-SUMMARY.md`
</output>