---
phase: "01-foundation-core-ui"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "VitalPaw/src/context/AuthContext.tsx"
  - "VitalPaw/src/navigation/AppNavigator.tsx"
  - "VitalPaw/App.tsx"
autonomous: false
requirements:
  - "AUTH-01"
  - "AUTH-02"
  - "AUTH-03"
  - "AUTH-04"
  - "UI-06"
  - "TECH-02"

must_haves:
  truths:
    - "User can create account with email and password"
    - "User receives email verification after signup"
    - "User can log in and stay logged in across sessions"
    - "User can log out from any page"
    - "Tab navigation shows visual feedback on selection"
  artifacts:
    - path: "VitalPaw/src/context/AuthContext.tsx"
      provides: "Authentication state management with signUp/signIn/signOut"
      exports: ["AuthProvider", "useAuth"]
    - path: "VitalPaw/src/navigation/AppNavigator.tsx"
      provides: "Tab navigation with visual feedback"
      exports: ["AppNavigator"]
    - path: "VitalPaw/App.tsx"
      provides: "App entry point with auth provider"
  key_links:
    - from: "App.tsx"
      to: "AuthContext.tsx"
      via: "AuthProvider wrapper"
    - from: "AppNavigator.tsx"
      to: "AuthContext.tsx"
      via: "useAuth hook for isAuthenticated"
---

<objective>
Establish authentication foundation and verify tab navigation visual feedback. This plan covers the core auth flows (signup, login, session persistence, logout) and ensures tab navigation has proper visual indicators per UI-06.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
## Existing Code Structure

### AuthContext.tsx (already implemented)
- signUp, signIn, signOut functions using SecureStore for session persistence
- MOCK_USERS in-memory store (needs conversion to proper user registration flow)
- Email verification simulated via console.log (needs actual implementation)

### AppNavigator.tsx (already implemented)
- Bottom tab navigator with 4 tabs: Dashboard, Monitoring, History, Profile
- Tab icons using Ionicons with focus-based icon switching (home/home-outline, etc.)
- tabBarActiveTintColor and tabBarInactiveTintColor for color feedback
- tabBarLabelStyle for typography

### App.tsx (verify structure)
- Should wrap with AuthProvider and NavigationContainer

## Interface Contracts

From src/types/index.ts:
```typescript
export interface User {
  id: string;
  email: string;
  name: string;
  createdAt: Date;
}

export interface AuthState {
  user: User | null;
  isAuthenticated: boolean;
  isLoading: boolean;
  error: string | null;
}
```

## Task Specifications

### Task 1: Verify and Enhance Auth Flow
**Files:** `VitalPaw/src/context/AuthContext.tsx`

**Action:**
1. Read the existing AuthContext.tsx to understand current implementation
2. The current implementation uses in-memory MOCK_USERS which won't persist across app restarts
3. Implement proper user registration flow that persists to AsyncStorage:
   - Add new user registration that stores `{email, passwordHash, userData}` in AsyncStorage
   - Use bcrypt-style password hashing simulation (or crypto primitive) for security
   - Create `USERS_STORAGE_KEY` in constants for user data storage
4. Implement email verification flow:
   - Add `emailVerified` field to User type
   - After signup, set `emailVerified: false` and generate a verification code
   - Store verification codes in AsyncStorage mapped to email
   - Add `verifyEmail(code: string)` function to AuthContext
   - Show verification status in Profile screen
5. Ensure session persistence works: after signIn, user data stored in SecureStore
6. Add `isEmailVerified` getter to AuthContext that returns user?.emailVerified ?? false
7. Strip all console.log statements per TECH-02

**Verify:**
- Unit test that new user can sign up and data persists after app restart
- Unit test that email verification can be completed with valid code

### Task 2: Add Tab Navigation Visual Feedback Enhancement
**Files:** `VitalPaw/src/navigation/AppNavigator.tsx`

**Action:**
1. Read existing AppNavigator.tsx
2. Enhance tab bar to include animated underline indicator on active tab:
   - Add custom tabBarIndicatorStyle that shows teal underline (3px height, rounded)
   - Use `@react-native-async-viewing/animated` for smooth transition or simpler React Native Animated API
   - The indicator should animate position when switching tabs
3. Verify colors match design: active = COLORS.primary (teal), inactive = COLORS.gray400
4. Add slight scale animation (1.0 -> 1.05) on active tab icon
5. Ensure keyboard dismiss behavior on tab press

**Verify:**
- Visual inspection shows animated underline sliding between tabs
- Active tab icon is slightly larger (scale 1.05)
- Inactive tabs show outline variant of icons

### Task 3: Create Human Verification Checkpoint
**Type:** checkpoint:human-verify
**Gate:** blocking

**What Built:**
- Full authentication flow (sign up, email verification, log in, log out)
- Tab navigation with animated visual feedback

**How to Verify:**
1. Open app in Expo/ simulator
2. Sign up with a new email - confirm "Verification Email Sent" alert appears
3. Complete email verification (check AsyncStorage for verification code if mock)
4. Log out and log back in - confirm session persists
5. Navigate through all 4 tabs - observe:
   - Teal underline slides to active tab
   - Active icon is filled variant, slightly larger
   - Inactive icons show outline variant
6. Tap logout on Dashboard - confirm return to auth screen

**Resume Signal:** Type "approved" or describe issues

### Task 4: Strip Console.log Statements (TECH-02)
**Files:** All TypeScript files in VitalPaw/src

**Action:**
1. Search for all console.log statements in src/ directory
2. Remove or replace with proper error tracking:
   - Replace `console.log` used for debugging with proper logging mechanism
   - For errors, use Sentry or similar (or just remove if no error tracking configured)
3. Specifically in AuthContext.tsx line 91: `console.log(\`Verification email sent to ${email}\`)` - remove or mark as placeholder for email service
4. Verify no console.log statements remain in production code

**Verify:**
- `grep -r "console.log" VitalPaw/src --include="*.ts" --include="*.tsx"` returns no results
</verify>
- `grep -r "console.log" VitalPaw/src --include="*.ts" --include="*.tsx"` returns no results

**Done:**
- AUTH-01: User can create account with email and password (verified)
- AUTH-02: User receives email verification after signup (mock implemented)
- AUTH-03: User can log in and stay logged in across sessions (verified)
- AUTH-04: User can log out from any page (verified)
- UI-06: Tab navigation shows visual feedback on selection (animated underline + scale)
- TECH-02: Console.log stripped from production builds (verified)
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → AuthContext | Untrusted user input (email, password) crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|------------|-----------------|
| T-01-01 | Tampering | AuthContext | mitigate | Validate email format with regex before storage, hash passwords |
| T-01-02 | Information Disclosure | AuthContext | mitigate | Store only hashed passwords, never plain text |
| T-01-03 | Denial of Service | AuthContext | mitigate | Limit signup attempts, clear error messages |
</threat_model>

<verification>
- Run unit tests: `npm test -- --testPathPattern="AuthContext" --passWithNoTests`
- Verify tab navigation renders without errors
- Confirm no console.log statements remain via grep
</verification>

<success_criteria>
- [ ] User can sign up with email/password and data persists
- [ ] Email verification flow is triggered on signup (mock acceptable)
- [ ] User stays logged in after app restart (session persistence verified)
- [ ] Logout button works from Dashboard
- [ ] Tab navigation shows animated teal underline on active tab
- [ ] All console.log statements removed from src/
- [ ] Checkpoint approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-ui/01-01-SUMMARY.md`
</output>