---
phase: "01-foundation-core-ui"
plan: "03"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "VitalPaw/src/components/Card.tsx"
  - "VitalPaw/src/components/MetricDisplay.tsx"
  - "VitalPaw/src/components/ActionButton.tsx"
  - "VitalPaw/src/components/InputForm.tsx"
  - "VitalPaw/src/screens/DashboardScreen.tsx"
autonomous: true
requirements:
  - "UI-01"
  - "UI-02"
  - "UI-03"
  - "UI-05"
  - "DASH-01"
  - "DASH-02"
  - "DASH-03"
  - "DASH-04"
  - "DASH-05"
  - "PROF-01"

must_haves:
  truths:
    - "Card component renders with title and children"
    - "MetricDisplay shows label, value, unit, trend, status"
    - "ActionButton supports primary/secondary variants with icon"
    - "InputForm handles all input types with validation error display"
    - "Dashboard displays pet greeting with status summary"
    - "Dashboard shows top 3-4 key metrics (weight, activity, condition)"
    - "Dashboard shows alert cards for important notifications"
    - "Dashboard provides quick action buttons for common tasks"
  artifacts:
    - path: "VitalPaw/src/components/Card.tsx"
      provides: "Card container with title support"
    - path: "VitalPaw/src/components/MetricDisplay.tsx"
      provides: "Single-value metric display with trend/status"
    - path: "VitalPaw/src/components/ActionButton.tsx"
      provides: "Primary/secondary action buttons with icons"
    - path: "VitalPaw/src/components/InputForm.tsx"
      provides: "Form input with label, validation, error display"
---

<objective>
Verify and enhance UI components (Card, MetricDisplay, ActionButton, InputForm) and ensure DashboardScreen implements all DASH requirements (pet greeting, key metrics, alert cards, quick actions) and ProfileScreen supports PROF-01 (account settings management).
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
## Existing Component Implementations

### Card.tsx
- Props: title?, children, style?
- Renders View with shadow, rounded corners
- Title is optional Text element

### MetricDisplay.tsx
- Props: label, value, unit?, trend?, status?
- Shows label (uppercase), value (large, colored by status), optional unit
- Trend icon (trending-up/down/remove) colored by status
- Status colors: normal=green, warning=amber, critical=red

### ActionButton.tsx
- Props: title, onPress, variant?, disabled?, icon?, style?
- Primary: teal background, white text
- Secondary: transparent with teal border and text
- Supports Ionicons icon (keyof Ionicons.glyphMap)
- Disabled state with gray styling

### InputForm.tsx
- Props: label, value, onChangeText, placeholder?, keyboardType?, secureTextEntry?, error?, style?
- TextInput with label above
- Error text below when error prop provided
- Supports: default, email-address, numeric, phone-pad keyboard types

### DashboardScreen.tsx
Already implements:
- Pet greeting with time-based message (getGreeting())
- Pet selector with horizontal scroll
- Pet summary showing name, age, weight, species
- Key metrics section with 3 MetricDisplay components (Weight, Activity, Condition)
- Alert cards with icons and color coding
- Quick action buttons (Log Weight, Log Activity, Medication, Vaccination)
- Modal for Add/Edit Pet

### ProfileScreen.tsx
Already implements:
- Profile card with avatar, name, email
- Account settings list (Edit Profile, Change Password, Email Verification)
- Edit profile modal with name/email form
- Sign out functionality
- Delete account option

## What Needs Enhancement

1. **Card**: Add proper TypeScript interface export for use elsewhere
2. **MetricDisplay**: Verify accessibility labels for screen readers
3. **ActionButton**: May need loading state indicator
4. **InputForm**: May need multiline support
5. **Dashboard**: All DASH requirements appear implemented in current code
6. **Profile**: PROF-01 (account settings) already implemented

## Task Specifications

### Task 1: Verify UI Component Exports and Accessibility
**Files:** `VitalPaw/src/components/*.tsx`

**Action:**
1. Read each component file
2. Ensure all components have proper React.FC typing with explicit interfaces
3. Add accessibility labels (accessibilityLabel prop) to interactive elements
4. Add accessibility hints where appropriate
5. Ensure component exports match the index.ts barrel export
6. Strip any console.log statements (TECH-02)

**Verify:**
- `npm test -- --testPathPattern="components" --passWithNoTests`
- All components compile without TypeScript errors

### Task 2: Verify Dashboard Implements All DASH Requirements
**Files:** `VitalPaw/src/screens/DashboardScreen.tsx`

**Action:**
1. Read DashboardScreen.tsx
2. Verify DASH-01: Pet greeting (getGreeting function returns "Good morning/afternoon/evening")
3. Verify DASH-02: Top 3-4 key metrics (Weight, Activity, Condition in metricsRow)
4. Verify DASH-03: Alert cards (alerts array mapped to alertCard components with icons)
5. Verify DASH-04: Quick action buttons (4 buttons: Log Weight, Log Activity, Medication, Vaccination)
6. Verify DASH-05: Recent activity feed - currently shows charts, needs explicit "recent activity" section with entries from healthStore
7. Ensure all data comes from proper stores (usePetStore, useHealthStore)
8. Strip any console.log statements

**Verify:**
- Dashboard renders all required sections
- Data loads from stores correctly
- No TypeScript errors

### Task 3: Verify Profile Implements PROF-01
**Files:** `VitalPaw/src/screens/ProfileScreen.tsx`

**Action:**
1. Read ProfileScreen.tsx
2. Verify PROF-01: "User can manage account settings (email, password)"
   - Edit Profile modal allows name and email changes
   - Change Password option exists (UI present, may not be functional yet - that's okay for Phase 1)
   - Email verification status shown
3. Ensure updateUser function properly updates auth context
4. Verify signOut works (PROF-01 implies user can manage their session)
5. Strip any console.log statements

**Verify:**
- Profile screen renders with all settings sections
- Edit profile modal updates user data
- Sign out navigates back to auth screen

### Task 4: Create Component Test Stub
**Files:** `VitalPaw/src/components/__tests__/`

**Action:**
1. Create test files for Card, MetricDisplay, ActionButton, InputForm
2. Test Card renders children and optional title
3. Test MetricDisplay renders all props correctly
4. Test ActionButton primary/secondary variants and disabled state
5. Test InputForm error state and different keyboard types

**Verify:**
- `npm test -- --testPathPattern="components"`
</task>

<verification>
- All components render correctly
- Dashboard shows pet greeting, metrics, alerts, quick actions
- Profile allows account settings management
- No console.log statements remain
</verification>

<success_criteria>
- [ ] Card renders with title and children
- [ ] MetricDisplay shows label, value, unit, trend, status
- [ ] ActionButton primary/secondary variants work
- [ ] InputForm handles all keyboard types with error display
- [ ] Dashboard displays greeting, metrics (3-4), alerts, quick actions
- [ ] Profile allows account settings management
- [ ] No console.log statements in src/
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-ui/01-03-SUMMARY.md`
</output>