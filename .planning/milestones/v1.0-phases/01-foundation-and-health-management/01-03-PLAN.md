---
phase: 01-foundation-and-health-management
plan: 03
type: execute
wave: 2
depends_on:
  - 01-02-PLAN
files_modified:
  - PetcareApp/app/(tabs)/health/[petId]/index.tsx
  - PetcareApp/app/(tabs)/health/[petId]/add.tsx
  - PetcareApp/app/(tabs)/health/[petId]/[recordId].tsx
  - PetcareApp/src/services/health.service.ts
  - PetcareApp/src/contexts/HealthContext.tsx
  - PetcareApp/src/types/health.types.ts
autonomous: true
requirements:
  - HEAL-01
  - HEAL-02
  - HEAL-03
  - HEAL-04
must_haves:
  truths:
    - 사용자가 예방접종 기록 가능
    - 사용자가 약물 일정 기록 가능
    - 사용자가 진료 이력 기록 가능
    - 사용자가 각 반려동물의 건강 기록 타임라인 조회 가능
  artifacts:
    - path: PetcareApp/src/contexts/HealthContext.tsx
      provides: 건강 기록 상태 관리
    - path: PetcareApp/app/(tabs)/health/[petId]/index.tsx
      provides: 건강 기록 타임라인 화면
    - path: PetcareApp/src/types/health.types.ts
      provides: 건강 기록 타입 정의
  key_links:
    - from: PetcareApp/app/(tabs)/health/[petId]/index.tsx
      to: PetcareApp/src/contexts/HealthContext.tsx
      via: 건강 기록 조회
    - from: PetcareApp/app/(tabs)/health/[petId]/add.tsx
      to: PetcareApp/src/services/health.service.ts
      via: 건강 기록 생성 API 호출
---

<objective>
Phase 1을 위한 건강 기록 관리 생성. 예방접종, 약물, 진료 기록 및 타임라인 보기 처리.

목적: 핵심 건강 추적 기능 — 앱의 주요 가치 제안.
산출물: HealthContext, 건강 기록 화면, API 서비스
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

# 요구사항:
# - HEAL-01: 예방접종 이력 기록 (vaccine명, 날짜, 다음 만료일)
# - HEAL-02: 약물 일정 기록 (약물명, 용량, 빈도, 날짜)
# - HEAL-03: 진료 이력 기록 (병원명, 날짜, 진단, 메모)
# - HEAL-04: 각 반려동물별 모든 건강 기록의 연대순 타임라인 조회

# React Native Expo 사용:
# - expo-router 사용 (file-based routing)
# - expo-image-picker로 사진 첨부 (진단서 등)
</context>

<tasks>

<task type="auto">
  <name>Task 1: health.types.ts 생성 (건강 기록 타입)</name>
  <files>PetcareApp/src/types/health.types.ts</files>
  <action>
건강 기록 타입 정의 생성:
- HealthRecordType: enum { VACCINATION, MEDICATION, EXAMINATION }

- VaccinationInput: { vaccineName, dateAdministered, nextDueDate? }
- MedicationInput: { medicineName, dosage, frequency, startDate, endDate? }
- ExaminationInput: { clinicName, diagnosis, notes? }

- HealthRecord: { id, petId, type, recordDate, data: VaccinationInput | MedicationInput | ExaminationInput, createdAt }

- CreateHealthRecordInput: { type, recordDate, data }
  </action>
  <verify>
    <automated>grep -l "HealthRecordType\|VaccinationInput" PetcareApp/src/types/health.types.ts</automated>
  </verify>
  <done>건강 기록 타입 내보내짐</done>
</task>

<task type="auto">
  <name>Task 2: health.service.ts API 서비스 생성</name>
  <files>PetcareApp/src/services/health.service.ts</files>
  <action>
API 서비스 생성:
- getHealthTimeline(petId: string, filters?: {type?, fromDate?, toDate?}): Promise<HealthRecord[]>
- getHealthRecordById(petId: string, recordId: string): Promise<HealthRecord>
- createHealthRecord(petId: string, input: CreateHealthRecordInput): Promise<HealthRecord>
- updateHealthRecord(petId: string, recordId: string, input: CreateHealthRecordInput): Promise<HealthRecord>
- deleteHealthRecord(petId: string, recordId: string): Promise<void>
- getUpcomingVaccinations(petId: string, daysAhead: number): Promise<HealthRecord[]>
- getActiveMedications(petId: string): Promise<HealthRecord[]>

역시 pet.service.ts와 동일한 구조.
  </action>
  <verify>
    <automated>grep -l "getHealthTimeline\|createHealthRecord" PetcareApp/src/services/health.service.ts</automated>
  </verify>
  <done>health.service가 모든 CRUD 함수 내보냄</done>
</task>

<task type="auto">
  <name>Task 3: HealthContext 생성 (건강 기록 상태 관리)</name>
  <files>PetcareApp/src/contexts/HealthContext.tsx</files>
  <action>
React Context로 HealthContext 생성:
- records: HealthRecord[] (건강 기록 목록)
- isLoading: boolean
- fetchTimeline(petId: string, filters?): Promise<void>
- addRecord(petId: string, input: CreateHealthRecordInput): Promise<HealthRecord>
- updateRecord(petId: string, recordId: string, input: CreateHealthRecordInput): Promise<HealthRecord>
- removeRecord(petId: string, recordId: string): Promise<void>

현재 선택된 반려동물(petId)을 기반으로 기록 관리.
  </action>
  <verify>
    <automated>grep -l "HealthContext\|records\|fetchTimeline" PetcareApp/src/contexts/HealthContext.tsx</automated>
  </verify>
  <done>HealthContext가 records와 CRUD 함수 내보냄</done>
</task>

<task type="auto">
  <name>Task 4: 건강 기록 타임라인 화면 생성</name>
  <files>PetcareApp/app/(tabs)/health/[petId]/index.tsx</files>
  <action>
건강 기록 타임라인 화면 생성:
- 연대순 정렬 (최신순) FlatList
- 각 항목: 아이콘 (유형별), 제목, 날짜, 요약
- 필터: 전체 / 예방접종 / 약물 / 진료 (DropDown 또는 Tab)
- "+" 버튼으로 새 기록 추가 화면으로 이동
- 항목 클릭 시 상세/수정 화면으로 이동
- 빈 상태: "건강 기록을 등록해주세요"
- Pull-to-refresh 지원

record.type에 따라 다른 아이콘 표시.
  </action>
  <verify>
    <automated>grep -l "FlatList\|HealthRecordCard\|useHealth" PetcareApp/app/\(tabs\)/health/[petId]/index.tsx</automated>
  </verify>
  <done>건강 기록 타임라인이 연대순으로 표시되고 필터링 가능</done>
</task>

<task type="auto">
  <name>Task 5: 건강 기록 추가 화면 생성</name>
  <files>PetcareApp/app/(tabs)/health/[petId]/add.tsx</files>
  <action>
건강 기록 추가 화면 생성:
- 기록 유형 선택 (预防接종 / 약물 / 진료)
- 유형에 따라 다른 Form Fields 표시:

[预防接종]
- vaccineName: TextInput (필수)
- dateAdministered: DatePicker (필수)
- nextDueDate: DatePicker (선택)

[약물]
- medicineName: TextInput (필수)
- dosage: TextInput (필수)
- frequency: DropDown (매일, 주 1회, 주 2회, 월 1회 등)
- startDate: DatePicker (필수)
- endDate: DatePicker (선택)

[진료]
- clinicName: TextInput (필수)
- diagnosis: TextInput (필수)
- notes: TextArea (선택)
- 사진 첨부 버튼 (진단서 사진)

- "저장" 버튼
  </action>
  <verify>
    <automated>grep -l "TextInput\|DatePicker\|Picker" PetcareApp/app/\(tabs\)/health/[petId]/add.tsx</automated>
  </verify>
  <done>건강 기록 추가 화면이 유형별 Form 표시</done>
</task>

<task type="auto">
  <name>Task 6: 건강 기록 상세/수정 화면 생성</name>
  <files>PetcareApp/app/(tabs)/health/[petId]/[recordId].tsx</files>
  <action>
건강 기록 상세/수정 화면 생성:
- 기록 유형 및 데이터 표시 (읽기 전용 또는 수정 모드)
- "수정" 버튼 → 수정 모드로 전환
- "삭제" 버튼 (확인 대화상자 포함)
- 수정 후 저장 시 API 호출

현재 petId와 recordId 사용.
  </action>
  <verify>
    <automated>grep -l "Text\|Button\|updateRecord" PetcareApp/app/\(tabs\)/health/[petId]/[recordId].tsx</automated>
  </verify>
  <done>건강 기록 상세/수정 화면이 데이터 표시 및 수정/삭제 가능</done>
</task>

</tasks>

<verification>
1. 타임라인: GET /api/pets/[id]/health하면 날짜순 기록 반환
2. 추가: 예방접종/약물/진료 기록 추가 후 타임라인에 표시
3. 수정: 기록 수정 후 반영
4. 삭제: 기록 삭제 후 타임라인에서 제거
5. 필터: 유형별 필터링 가능
</verification>

<success_criteria>
- 사용자가 세 가지 타입의 건강 데이터 모두 기록 가능
- 타임라인 보기가 날짜순으로 정렬된 모든 기록 표시
- 기록 타입별 필터링 작동
</success_criteria>

<output>
완료 후 .planning/phases/01-foundation-and-health-management/01-03-SUMMARY.md 생성
</output>