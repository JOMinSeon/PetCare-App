---
phase: 01-foundation-and-health-management
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02-PLAN
  - 01-03-PLAN
files_modified:
  - PetcareApp/app/(tabs)/reminders.tsx
  - PetcareApp/app/(tabs)/reminder/add.tsx
  - PetcareApp/app/(tabs)/reminder/edit/[id].tsx
  - PetcareApp/src/services/notification.service.ts
  - PetcareApp/src/contexts/ReminderContext.tsx
  - PetcareApp/src/types/notification.types.ts
  - PetcareApp/src/utils/scheduler.ts
autonomous: true
requirements:
  - NOTI-01
  - NOTI-02
  - NOTI-03
  - NOTI-04
must_haves:
  truths:
    - 사용자가 약물 리마인더 설정 가능
    - 사용자가 예방접종 만료일 알림 수신
    - 사용자가 산책 시간 알림 수신
    - 사용자가 사용자 정의 리마인더 생성 가능
  artifacts:
    - path: PetcareApp/src/contexts/ReminderContext.tsx
      provides: 리마인더 상태 관리
    - path: PetcareApp/app/(tabs)/reminders.tsx
      provides: 리마인더 목록 화면
    - path: PetcareApp/src/services/notification.service.ts
      provides: 푸시 알림 및 스케줄링 서비스
  key_links:
    - from: PetcareApp/app/(tabs)/reminders.tsx
      to: PetcareApp/src/contexts/ReminderContext.tsx
      via: 리마인더 목록 조회
    - from: PetcareApp/src/services/notification.service.ts
      to: PetcareApp/src/services/health.service.ts
      via: upcoming vaccinations, active medications 조회
---

<objective>
Phase 1을 위한 알림 및 리마인더 시스템 생성. 푸시 알림 및 사용자 정의 리마인더 처리.

목적: 적시 리마인더는 핵심 가치 제안 — PROJECT.md에 따르면 알림은 99%+ 정확도가 필요합니다.
산출물: ReminderContext, 리마인더 화면, 푸시 알림 및 스케줄링 서비스
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

# 요구사항:
# - NOTI-01: 예약된 약물에 대한 푸시 리마인더
# - NOTI-02: 예방접종 만료일 다가올 때 푸시
# - NOTI-03: 예약된 산책 시간에 대한 푸시
# - NOTI-04: 건강 관련 작업에 대한 사용자 정의 리마인더

# React Native Expo 사용:
# - expo-notifications으로 푸시 알림
# - expo-task-manager로 백그라운드タスク
# - AsyncStorage에 리마인더 저장 (오프라인 대응)
</context>

<tasks>

<task type="auto">
  <name>Task 1: notification.types.ts 생성 (알림 타입)</name>
  <files>PetcareApp/src/types/notification.types.ts</files>
  <action>
알림 타입 정의 생성:
- ReminderType: enum { MEDICATION, VACCINATION, WALK, CUSTOM }
- RepeatPattern: enum { NONE, DAILY, WEEKLY, MONTHLY }

- Reminder: { id, petId?, userId, type, title, message, triggerDate, triggerTime, repeatPattern, isActive, createdAt }

- CreateReminderInput: { petId?, type, title, message, triggerDate, triggerTime, repeatPattern? }
- UpdateReminderInput: { title?, message?, triggerDate?, triggerTime?, repeatPattern?, isActive? }

- DeviceToken: { id, token, platform: 'ios' | 'android' }
  </action>
  <verify>
    <automated>grep -l "ReminderType\|interface Reminder" PetcareApp/src/types/notification.types.ts</automated>
  </verify>
  <done>알림 타입 내보내짐</done>
</task>

<task type="auto">
  <name>Task 2: notification.service.ts API 서비스 생성</name>
  <files>PetcareApp/src/services/notification.service.ts</files>
  <action>
API 서비스 생성:
- getReminders(petId?: string): Promise<Reminder[]>
- getReminderById(id: string): Promise<Reminder>
- createReminder(input: CreateReminderInput): Promise<Reminder>
- updateReminder(id: string, input: UpdateReminderInput): Promise<Reminder>
- deleteReminder(id: string): Promise<void>
- registerDeviceToken(token: string, platform: string): Promise<DeviceToken>
- unregisterDeviceToken(token: string): Promise<void>

로컬 저장소 (AsyncStorage)에도 리마인더 캐시.
  </action>
  <verify>
    <automated>grep -l "getReminders\|createReminder" PetcareApp/src/services/notification.service.ts</automated>
  </verify>
  <done>notification.service가 모든 CRUD 함수 내보냄</done>
</task>

<task type="auto">
  <name>Task 3: ReminderContext 생성 (리마인더 상태 관리)</name>
  <files>PetcareApp/src/contexts/ReminderContext.tsx</files>
  <action>
React Context로 ReminderContext 생성:
- reminders: Reminder[] (사용자의 리마인더 목록)
- isLoading: boolean
- fetchReminders(petId?: string): Promise<void>
- addReminder(input: CreateReminderInput): Promise<Reminder>
- updateReminder(id: string, input: UpdateReminderInput): Promise<Reminder>
- removeReminder(id: string): Promise<void>
- toggleReminder(id: string): Promise<void> (활성/비활성 토글)

로컬 캐시도 관리 (오프라인 대응).
  </action>
  <verify>
    <automated>grep -l "ReminderContext\|reminders\|fetchReminders" PetcareApp/src/contexts/ReminderContext.tsx</automated>
  </verify>
  <done>ReminderContext가 reminders와 CRUD 함수 내보냄</done>
</task>

<task type="auto">
  <name>Task 4: scheduler.ts 유틸리티 (로컬 알림 스케줄링)</name>
  <files>PetcareApp/src/utils/scheduler.ts</files>
  <action>
로컬 알림 스케줄링 유틸리티 생성:
- scheduleNotification(reminder: Reminder): Promise<void>
- cancelNotification(reminderId: string): Promise<void>
- cancelAllNotifications(): Promise<void>
- checkUpcomingVaccinations(): Promise<void> (백그라운드에서 호출)
- checkActiveMedications(): Promise<void> (백그라운드에서 호출)

expo-notifications 사용:
- NotificationTrigger (로컬 알림)
- *가상의 백그라운드 실행 (실제론 foreground로만 동작)

실제 백그라운드 푸시 알림은 서버에서 처리 (NOTI-01, NOTI-02는 서버 스케줄링).
NOTI-03, NOTI-04는 로컬에서 처리 가능.
  </action>
  <verify>
    <automated>grep -l "scheduleNotification\|cancelNotification" PetcareApp/src/utils/scheduler.ts</automated>
  </verify>
  <done>scheduler.ts가 로컬 알림 스케줄링 함수 내보냄</done>
</task>

<task type="auto">
  <name>Task 5: 리마인더 목록 화면 생성</name>
  <files>PetcareApp/app/(tabs)/reminders.tsx</files>
  <action>
리마인더 목록 화면 생성:
- 전체 리마인더 FlatList (날짜순)
- 각 카드: 아이콘 (유형별), 제목, 시간, 반복 패턴, 활성/비활성 상태
- 필터: 전체 / 약물 / 예방접종 / 산책 / 사용자 정의
- "+" 버튼으로 새 리마인더 추가
- 카드 클릭 시 수정 화면으로 이동
- 활성/비활성 토글 스위치
- Pull-to-refresh
- 빈 상태: "리마인더를 등록해주세요"
  </action>
  <verify>
    <automated>grep -l "FlatList\|ReminderCard\|useReminder" PetcareApp/app/\(tabs\)/reminders.tsx</automated>
  </verify>
  <done>리마인더 목록이 표시되고 필터링/토글 가능</done>
</task>

<task type="auto">
  <name>Task 6: 리마인더 추가 화면 생성</name>
  <files>PetcareApp/app/(tabs)/reminder/add.tsx</files>
  <action>
리마인더 추가 화면 생성:
- 반려동물 선택 (DropDown, 선택사항)
- 유형 선택 (MEDICATION, VACCINATION, WALK, CUSTOM)
- 제목 입력 (필수)
- 메시지 입력 (선택)
- 날짜 선택 (DatePicker)
- 시간 선택 (TimePicker)
- 반복 패턴 선택 (NONE, DAILY, WEEKLY, MONTHLY)
- "저장" 버튼

유형별 기본 제목/메시지 제공 (예: 약물 복용 알림).
  </action>
  <verify>
    <automated>grep -l "TextInput\|DatePicker\|TimePicker\|Picker" PetcareApp/app/\(tabs\)/reminder/add.tsx</automated>
  </verify>
  <done>리마인더 추가 화면이 모든 필드 제공</done>
</task>

<task type="auto">
  <name>Task 7: 리마인더 수정 화면 생성</name>
  <files>PetcareApp/app/(tabs)/reminder/edit/[id].tsx</files>
  <action>
리마인더 수정 화면 생성:
- 기존 데이터 pre-fill
- 모든 필드 수정 가능
- 활성/비활성 토글
- "저장" 버튼
- "삭제" 버튼 (확인 대화상자)
  </action>
  <verify>
    <automated>grep -l "TextInput\|updateReminder\|deleteReminder" PetcareApp/app/\(tabs\)/reminder/edit/[id].tsx</automated>
  </verify>
  <done>리마인더 수정 화면이 기존 데이터 표시 및 저장/삭제 가능</done>
</task>

</tasks>

<verification>
1. 목록: 리마인더 목록이 표시
2. 추가: 새 리마인더 등록 후 목록에 표시
3. 수정: 리마인더 수정 후 반영
4. 삭제: 리마인더 삭제 후 목록에서 제거
5. 토글: 활성/비활성 상태 변경
6. 알림: 예약된 시간에 푸시 알림 발생
</verification>

<success_criteria>
- 사용자가 사용자 정의 리마인더 생성 가능
- 시스템이 약물 만료 시 푸시 알림 전송 (서버 연동)
- 시스템이 예방접종 만료일 다가올 때 푸시 알림 전송
- 사용자가 리마인더 관리 가능 (생성, 수정, 삭제, 토글)
- 로컬 알림이 예약된 시간에 발생
</success_criteria>

<output>
완료 후 .planning/phases/01-foundation-and-health-management/01-04-SUMMARY.md 생성
</output>