---
phase: 01-foundation-and-health-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - PetcareApp/app/(tabs)/pets.tsx
  - PetcareApp/app/(tabs)/pet/[id].tsx
  - PetcareApp/app/(tabs)/pet/add.tsx
  - PetcareApp/app/(tabs)/pet/edit/[id].tsx
  - PetcareApp/src/services/pet.service.ts
  - PetcareApp/src/contexts/PetContext.tsx
  - PetcareApp/src/types/pet.types.ts
autonomous: true
requirements:
  - PET-01
  - PET-02
  - PET-03
must_haves:
  truths:
    - 사용자가 이름, 종, 품종, 생년월일, 사진으로 반려동물 등록 가능
    - 사용자가 반려동물 프로필 정보 수정 가능
    - 사용자가 등록한 반려동물 목록 조회 가능
  artifacts:
    - path: PetcareApp/src/contexts/PetContext.tsx
      provides: 반려동물 상태 관리
    - path: PetcareApp/app/(tabs)/pets.tsx
      provides: 반려동물 목록 화면
    - path: PetcareApp/src/types/pet.types.ts
      provides: Pet 타입 정의
  key_links:
    - from: PetcareApp/app/(tabs)/pets.tsx
      to: PetcareApp/src/contexts/PetContext.tsx
      via: Pet 목록 조회
    - from: PetcareApp/app/(tabs)/pet/add.tsx
      to: PetcareApp/src/services/pet.service.ts
      via: 반려동물 생성 API 호출
---

<objective>
Phase 1을 위한 반려동물 프로필 관리 생성. 반려동물 등록, 수정, 조회 처리.

목적: 반려동물 프로필은 앱의 핵심 엔티티 — 모든 건강 기록은 반려동물에 연결됩니다.
산출물: PetContext, 반려동물 목록/상세/추가/수정 화면, API 서비스
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

# 요구사항:
# - PET-01: 이름, 종, 품종, 생년월일, 사진으로 반려동물 등록
# - PET-02: 반려동물 프로필 정보 수정
# - PET-03: 반려동물 목록 및 상세 조회

# React Native Expo 사용:
# - expo-router 사용 (file-based routing)
# - expo-image-picker로 사진 선택
# - expo-media-library 또는 expo-image-picker로 이미지 관리
</context>

<tasks>

<task type="auto">
  <name>Task 1: pet.types.ts 생성 (타입 정의)</name>
  <files>PetcareApp/src/types/pet.types.ts</files>
  <action>
타입 정의 생성:
- PetSpecies: enum { DOG, CAT, BIRD, FISH, REPTILE, OTHER }
- Pet: { id, userId, name, species, breed, birthDate, photoUrl, createdAt }
- CreatePetInput: { name, species, breed?, birthDate?, photoUrl? }
- UpdatePetInput: { name?, species?, breed?, birthDate?, photoUrl? }
  </action>
  <verify>
    <automated>grep -l "PetSpecies\|interface Pet" PetcareApp/src/types/pet.types.ts</automated>
  </verify>
  <done>Pet 타입 및 CreatePetInput, UpdatePetInput 내보내짐</done>
</task>

<task type="auto">
  <name>Task 2: pet.service.ts API 서비스 생성</name>
  <files>PetcareApp/src/services/pet.service.ts</files>
  <action>
API 서비스 생성:
- getPets(): Promise<Pet[]> - 사용자의 모든 반려동물 목록
- getPetById(id: string): Promise<Pet> - 단일 반려동물 상세
- createPet(input: CreatePetInput): Promise<Pet> - 반려동물 등록
- updatePet(id: string, input: UpdatePetInput): Promise<Pet> - 반려동물 수정
- deletePet(id: string): Promise<void> - 반려동물 삭제

auth.service.ts의 accessToken 사용 (Authorization 헤더).
  </action>
  <verify>
    <automated>grep -l "getPets\|createPet\|updatePet" PetcareApp/src/services/pet.service.ts</automated>
  </verify>
  <done>pet.service가 모든 CRUD 함수 내보냄</done>
</task>

<task type="auto">
  <name>Task 3: PetContext 생성 (반려동물 상태 관리)</name>
  <files>PetcareApp/src/contexts/PetContext.tsx</files>
  <action>
React Context로 PetContext 생성:
- pets: Pet[] (사용자의 반려동물 목록)
- selectedPet: Pet | null (선택된 반려동물)
- isLoading: boolean
- fetchPets(): Promise<void>
- selectPet(id: string): Promise<void>
- addPet(input: CreatePetInput): Promise<Pet>
- updatePet(id: string, input: UpdatePetInput): Promise<Pet>
- removePet(id: string): Promise<void>

 mascota 목록을 관리하고 선택된 mascota 상세 정보 제공.
  </action>
  <verify>
    <automated>grep -l "PetContext\|pets\|fetchPets" PetcareApp/src/contexts/PetContext.tsx</automated>
  </verify>
  <done>PetContext가 pets, selectedPet, CRUD 함수 내보냄</done>
</task>

<task type="auto">
  <name>Task 4: 반려동물 목록 화면 생성</name>
  <files>PetcareApp/app/(tabs)/pets.tsx</files>
  <action>
반려동물 목록 화면 생성:
- 등록된 반려동물 Grid/List 표시
- 각 카드: 사진, 이름, 종, 품종
- "+" 버튼으로 새 반려동물 추가 화면으로 이동
- 카드 클릭 시 상세 화면으로 이동
- 빈 상태: "반려동물을 등록해주세요" 메시지
- Pull-to-refresh 지원

usePet hook 사용 (PetContext에서 파생).
  </action>
  <verify>
    <automated>grep -l "FlatList\|PetCard\|usePet" PetcareApp/app/\(tabs\)/pets.tsx</automated>
  </verify>
  <done>반려동물 목록이 Grid/List로 표시되고 카드 클릭 시 상세 이동</done>
</task>

<task type="auto">
  <name>Task 5: 반려동물 상세 화면 생성</name>
  <files>PetcareApp/app/(tabs)/pet/[id].tsx</files>
  <action>
반려동물 상세 화면 생성:
- 대형 사진 (또는 기본 아이콘)
- 이름, 종, 품종, 생년월일 표시
- "건강 기록" 버튼 → 건강 기록 타임라인으로 이동
- "편집" 버튼 → 수정 화면으로 이동
- "삭제" 버튼 (확인 대화상자 포함)

usePet(id) hook로宠物 데이터 조회.
  </action>
  <verify>
    <automated>grep -l "Text\|Image\|Button" PetcareApp/app/\(tabs\)/pet/[id].tsx</automated>
  </verify>
  <done>반려동물 상세 화면이 모든 정보와 버튼 표시</done>
</task>

<task type="auto">
  <name>Task 6: 반려동물 추가 화면 생성</name>
  <files>PetcareApp/app/(tabs)/pet/add.tsx</files>
  <action>
반려동물 추가 화면 생성:
- 이름 입력 (필수)
- 종 선택 (DropDown: DOG, CAT, BIRD, FISH, REPTILE, OTHER)
- 품종 입력 (선택)
- 생년월일 선택 (DateTimePicker 또는 날짜 입력)
- 사진 선택 버튼 (expo-image-picker 사용)
- "등록" 버튼

Validación: 이름 필수, 종 필수.
  </action>
  <verify>
    <automated>grep -l "TextInput\|Button\|ImagePicker" PetcareApp/app/\(tabs\)/pet/add.tsx</automated>
  </verify>
  <done>반려동물 추가 화면이 모든 필드와 사진 선택 제공</done>
</task>

<task type="auto">
  <name>Task 7: 반려동물 수정 화면 생성</name>
  <files>PetcareApp/app/(tabs)/pet/edit/[id].tsx</files>
  <action>
반려동물 수정 화면 생성:
- 기존 데이터로 Form 채움 (pre-fill)
- 이름, 종, 품종, 생년월일, 사진 수정 가능
- "저장" 버튼
- 변경사항만 전송 (PATCH)

추가 화면과 동일한 레이아웃.
  </action>
  <verify>
    <automated>grep -l "TextInput\|Button\|ImagePicker" PetcareApp/app/\(tabs\)/pet/edit/[id].tsx</automated>
  </verify>
  <done>반려동물 수정 화면이 기존 데이터 표시 및 저장 가능</done>
</task>

</tasks>

<verification>
1. 목록: 반려동물 목록이 Grid로 표시
2. 상세: 반려동물 상세 정보 표시
3. 추가: 새 반려동물 등록 후 목록에 표시
4. 수정: 반려동물 정보 수정 후 반영
5. 삭제: 반려동물 삭제 후 목록에서 제거
6. 사진: 이미지 선택 및 업로드 가능
</verification>

<success_criteria>
- 사용자가 모든 필드로 반려동물 등록 가능
- 사용자가 자신의 반려동물 프로필 수정 가능
- 사용자가 반려동물 목록 조회 가능
- 사진 선택 및 업로드 가능
</success_criteria>

<output>
완료 후 .planning/phases/01-foundation-and-health-management/01-02-SUMMARY.md 생성
</output>