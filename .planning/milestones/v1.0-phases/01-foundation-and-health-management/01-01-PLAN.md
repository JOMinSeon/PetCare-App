---
phase: 01-foundation-and-health-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - PetcareApp/app/(auth)/signup.tsx
  - PetcareApp/app/(auth)/login.tsx
  - PetcareApp/app/(auth)/refresh.tsx
  - PetcareApp/src/contexts/AuthContext.tsx
  - PetcareApp/src/services/auth.service.ts
  - PetcareApp/src/hooks/useAuth.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
must_haves:
  truths:
    - 사용자가 이메일/비밀번호로 회원가입 가능
    - 사용자가 로그인하여 JWT 토큰 수신
    - 앱 재시작 시 사용자 세션 유지
  artifacts:
    - path: PetcareApp/src/contexts/AuthContext.tsx
      provides: 전역 인증 상태 관리 (React Context)
    - path: PetcareApp/app/(auth)/signup.tsx
      provides: 회원가입 화면
    - path: PetcareApp/app/(auth)/login.tsx
      provides: 로그인 화면
  key_links:
    - from: PetcareApp/app/(auth)/login.tsx
      to: PetcareApp/src/contexts/AuthContext.tsx
      via: AuthContext 사용 (useAuth hook)
    - from: PetcareApp/src/services/auth.service.ts
      to: PetcareApp/src/contexts/AuthContext.tsx
      via: 로그인/회원가입 API 호출
---

<objective>
Phase 1을 위한 인증 시스템 생성. 사용자 회원가입, 로그인, JWT 기반 세션 관리 처리.

목적: 다른 모든 기능은 인증된 사용자가 필요. 이것이 기초입니다.
산출물: AuthContext, 로그인/회원가입 화면, API 서비스
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

# 요구사항 참고:
# - AUTH-01: 이메일/비밀번호 회원가입 및 검증
# - AUTH-02: JWT 토큰 생성 로그인 (access + refresh)
# - AUTH-03: JWT 미들웨어 및 세션 유지

# React Native Expo 사용:
# - expo-router 사용 (file-based routing)
# - AsyncStorage로 토큰 저장
# - React Context로 인증 상태 관리
</context>

<tasks>

<task type="auto">
  <name>Task 1: AuthContext 생성 (전역 인증 상태)</name>
  <files>PetcareApp/src/contexts/AuthContext.tsx</files>
  <action>
React Context로 AuthContext 생성:
- user: User | null (현재 사용자)
- isLoading: boolean (인증 상태 로딩 중)
- isAuthenticated: boolean (로그인 상태)
- login(email, password): Promise<void>
- signup(email, password): Promise<void>
- logout(): Promise<void>
- refreshToken(): Promise<void>

AsyncStorage에 accessToken 및 refreshToken 저장.
앱 시작 시 refreshToken으로 자동 로그인 시도.
  </action>
  <verify>
    <automated>grep -l "AuthContext\|isAuthenticated" PetcareApp/src/contexts/AuthContext.tsx</automated>
  </verify>
  <done>AuthContext가 user, login, signup, logout 함수 내보냄</done>
</task>

<task type="auto">
  <name>Task 2: auth.service.ts API 서비스 생성</name>
  <files>PetcareApp/src/services/auth.service.ts</files>
  <action>
API 서비스 생성:
- signup(email: string, password: string): Promise<{user: User, accessToken: string}>
- login(email: string, password: string): Promise<{user: User, accessToken: string, refreshToken: string}>
- refreshAccessToken(): Promise<string>
- logout(): Promise<void>

API_BASE_URL는 환경 변수 사용 (개발: http://localhost:3000, 프로덕션: 실제 서버 URL)
axios 인스턴스로 API 호출.
  </action>
  <verify>
    <automated>grep -l "signup\|login\|refreshAccessToken" PetcareApp/src/services/auth.service.ts</automated>
  </verify>
  <done>auth.service가 signup, login, refreshAccessToken 함수 내보냄</done>
</task>

<task type="auto">
  <name>Task 3: useAuth hook 생성</name>
  <files>PetcareApp/src/hooks/useAuth.ts</files>
  <action>
AuthContext를 사용하는 useAuth hook 생성:
- AuthContext에서 user, isAuthenticated, isLoading, login, signup, logout 반환
- 다른 화면에서 쉽게 인증 상태 접근 가능

import { useAuth } from '@/contexts/AuthContext';
  </action>
  <verify>
    <automated>grep -l "useAuth\|AuthContext" PetcareApp/src/hooks/useAuth.ts</automated>
  </verify>
  <done>useAuth hook 내보내짐</done>
</task>

<task type="auto">
  <name>Task 4: 로그인 화면 생성</name>
  <files>PetcareApp/app/(auth)/login.tsx</files>
  <action>
expo-router를 사용한 로그인 화면 생성:
- Email 입력 필드
- Password 입력 필드 (보안)
- "로그인" 버튼
- "회원가입" 링크 (signup 화면으로 이동)
- 입력 검증 (이메일 형식, 비밀번호 8자 이상)
- 로딩 상태 표시
- 오류 메시지 표시

useAuth hook의 login 함수 사용.
  </action>
  <verify>
    <automated>grep -l "TextInput\|Button\|useAuth" PetcareApp/app/\(auth\)/login.tsx</automated>
  </verify>
  <done>로그인 화면이 이메일/비밀번호 입력과 버튼 제공</done>
</task>

<task type="auto">
  <name>Task 5: 회원가입 화면 생성</name>
  <files>PetcareApp/app/(auth)/signup.tsx</files>
  <action>
expo-router를 사용한 회원가입 화면 생성:
- Email 입력 필드
- Password 입력 필드 (보안)
- Password 확인 입력 필드
- "회원가입" 버튼
- "로그인" 링크 (login 화면으로 이동)
- 비밀번호 일치 검증
- 로딩 상태 표시
- 성공 시 로그인 화면으로 이동
  </action>
  <verify>
    <automated>grep -l "TextInput\|Button\|useAuth" PetcareApp/app/\(auth\)/signup.tsx</automated>
  </verify>
  <done>회원가입 화면이 이메일/비밀번호/확인 입력과 버튼 제공</done>
</task>

<task type="auto">
  <name>Task 6: Protected Route 처리</name>
  <files>PetcareApp/src/components/ProtectedRoute.tsx</files>
  <action>
ProtectedRoute 컴포넌트 생성:
- isAuthenticated가 false이면 로그인 화면으로 리다이렉트
- isLoading이 true이면 로딩 화면 표시
- 인증된 사용자만 자식 컴포넌트 렌더링

app/(tabs)/_layout.tsx에서 사용.
  </action>
  <verify>
    <automated>grep -l "ProtectedRoute\|isAuthenticated" PetcareApp/src/components/ProtectedRoute.tsx</automated>
  </verify>
  <done>ProtectedRoute 컴포넌트가 인증 상태 확인 및 리다이렉트</done>
</task>

</tasks>

<verification>
1. 회원가입: 유효한 이메일/비밀번호로 회원가입하면 성공
2. 로그인: 유효한 자격 증명으로 로그인하면 홈 화면으로 이동
3. 세션 유지: 앱을 닫고 다시 열면 로그인 상태 유지
4. 로그아웃: 로그아웃하면 로그인 화면으로 이동
</verification>

<success_criteria>
- 사용자가 회원가입하여 user 객체 수신
- 사용자가 로그인하여 JWT accessToken 수신
- AuthContext가 전역에서 인증 상태 제공
- 앱 재시작 시 세션 자동 복원
</success_criteria>

<output>
완료 후 .planning/phases/01-foundation-and-health-management/01-01-SUMMARY.md 생성
</output>