---
phase: 02-community-and-analytics
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01-PLAN
files_modified:
  - PetcareApp/app/(tabs)/health/[petId]/analytics.tsx
  - PetcareApp/app/(tabs)/health/[petId]/insights.tsx
  - PetcareApp/src/services/analytics.service.ts
  - PetcareApp/src/contexts/AnalyticsContext.tsx
  - PetcareApp/src/types/analytics.types.ts
autonomous: true
requirements:
  - ANLY-01
  - ANLY-02
  - ANLY-03
must_haves:
  truths:
    - 사용자가 각 반려동물의 체중 트렌드 차트 조회 가능
    - 사용자가 각 반려동물의 활동량 트렌드 차트 조회 가능
    - 사용자가 기록된 데이터 기반 기본 건강 인사이트 수신
  artifacts:
    - path: PetcareApp/src/contexts/AnalyticsContext.tsx
      provides: 건강 분석 상태 관리
    - path: PetcareApp/app/(tabs)/health/[petId]/analytics.tsx
      provides: 건강 트렌드 차트 화면
    - path: PetcareApp/src/types/analytics.types.ts
      provides: 분석 데이터 타입 정의
  key_links:
    - from: PetcareApp/app/(tabs)/health/[petId]/analytics.tsx
      to: PetcareApp/src/contexts/HealthContext.tsx
      via: 건강 기록 데이터 활용
---

<objective>
Phase 2를 위한 건강 분석 시스템 생성. 체중/활동량 트렌드 및 건강 인사이트 처리.

목적: 기록된 건강 데이터를 시각화하여 반려동물 건강 상태 파악 지원.
산출물: AnalyticsContext, 트렌드 차트 화면, 건강 인사이트
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# 요구사항:
# - ANLY-01: 체중 트렌드 차트 조회
# - ANLY-02: 활동량 트렌드 차트 조회
# - ANLY-03: 기본 건강 인사이트 제공

# React Native Expo 사용:
# - react-native-chart-kit 또는 victory-native로 차트 렌더링
# - 건강 기록 데이터 기반 분석
</context>

<tasks>

<task type="auto">
  <name>Task 1: analytics.types.ts 생성 (분석 타입)</name>
  <files>PetcareApp/src/types/analytics.types.ts</files>
  <action>
분석 타입 정의 생성:
- WeightDataPoint: { date: string, weight: number }
- ActivityDataPoint: { date: string, activityMinutes: number }
- HealthInsight: { type: 'weight' | 'activity' | 'medication' | 'vaccination', title: string, message: string, severity: 'info' | 'warning' | 'alert' }
- AnalyticsSummary: { petId, weightTrend: WeightDataPoint[], activityTrend: ActivityDataPoint[], insights: HealthInsight[] }
  </action>
  <verify>
    <automated>grep -l "WeightDataPoint\|HealthInsight" PetcareApp/src/types/analytics.types.ts</automated>
  </verify>
  <done>분석 타입 내보내짐</done>
</task>

<task type="auto">
  <name>Task 2: analytics.service.ts API 서비스 생성</name>
  <files>PetcareApp/src/services/analytics.service.ts</files>
  <action>
API 서비스 생성:
- getWeightTrend(petId: string): Promise<WeightDataPoint[]>
- getActivityTrend(petId: string): Promise<ActivityDataPoint[]>
- getHealthInsights(petId: string): Promise<HealthInsight[]>
- getAnalyticsSummary(petId: string): Promise<AnalyticsSummary>

서버에서 분석 데이터를 반환. 서버 미구현 시 로컬에서 임시 데이터 생성.
  </action>
  <verify>
    <automated>grep -l "getWeightTrend\|getHealthInsights" PetcareApp/src/services/analytics.service.ts</automated>
  </verify>
  <done>analytics.service가 모든 분석 함수 내보냄</done>
</task>

<task type="auto">
  <name>Task 3: AnalyticsContext 생성 (분석 상태 관리)</name>
  <files>PetcareApp/src/contexts/AnalyticsContext.tsx</files>
  <action>
React Context로 AnalyticsContext 생성:
- weightData: WeightDataPoint[]
- activityData: ActivityDataPoint[]
- insights: HealthInsight[]
- isLoading: boolean
- fetchAnalytics(petId: string): Promise<void>
- calculateLocalTrend(data: {date: string, value: number}[]): string (상승/하락/안정)
  </action>
  <verify>
    <automated>grep -l "AnalyticsContext\|weightData\|fetchAnalytics" PetcareApp/src/contexts/AnalyticsContext.tsx</automated>
  </verify>
  <done>AnalyticsContext가 분석 데이터와 Insights 내보냄</done>
</task>

<task type="auto">
  <name>Task 4: 건강 트렌드 차트 화면 생성</name>
  <files>PetcareApp/app/(tabs)/health/[petId]/analytics.tsx</files>
  <action>
건강 트렌드 차트 화면 생성:
- TabView 또는 SegmentedControl: 체중 / 활동량
- Line Chart로 트렌드 시각화 (react-native-chart-kit 사용)
- 날짜 범위 선택: 7일 / 30일 / 90일
- 현재값, 최소/최대, 평균 표시
- 트렌드 방향 표시 (↑ ↓ →)
- Pull-to-refresh

서버 미구현 시 mock 데이터로 차트 렌더링.
  </action>
  <verify>
    <automated>grep -l "LineChart\|Chart\|TabView" PetcareApp/app/\(tabs\)/health/[petId]/analytics.tsx</automated>
  </verify>
  <done>건강 트렌드 차트가 날짜 범위 선택과 함께 표시</done>
</task>

<task type="auto">
  <name>Task 5: 건강 인사이트 화면 생성</name>
  <files>PetcareApp/app/(tabs)/health/[petId]/insights.tsx</files>
  <action>
건강 인사이트 화면 생성:
- 알림 스타일 카드 리스트
- 각 카드: 아이콘, 제목, 메시지, 심각도 (info/warning/alert)
- 심각도별 색상 구분: 파랑/노랑/빨강
- 빈 상태: "충분한 데이터가 없습니다"
- 인사이트 히스토리 (过去的 알림들)
  </action>
  <verify>
    <automated>grep -l "FlatList\|InsightCard\|useAnalytics" PetcareApp/app/\(tabs\)/health/[petId]/insights.tsx</automated>
  </verify>
  <done>건강 인사이트가 카드 리스트로 표시</done>
</task>

</tasks>

<verification>
1. 차트: 체중/활동량 트렌드 차트 표시
2. 날짜 범위: 7일/30일/90일 선택 가능
3. 인사이트: 건강 인사이트 카드 조회
4. 시각화: 트렌드 방향 (상승/하락/안정) 표시
</verification>

<success_criteria>
- 사용자가 체중 트렌드 차트 조회 가능
- 사용자가 활동량 트렌드 차트 조회 가능
- 사용자가 건강 인사이트 카드로 정보 확인 가능
- 날짜 범위 선택으로 다양한 기간 데이터 조회 가능
</success_criteria>

<output>
완료 후 .planning/phases/02-community-and-analytics/02-02-SUMMARY.md 생성
</output>